# 2026-09-10

## Project: atr — algorithmic trading backend (IIFL Capital, NSE/BSE)

### Baseline established
- Repo had **zero commits**; created the initial commit (70 files, 9722 lines).
- `.gitignore` had no rules for `*.log`, `node_modules/`, or `web/dist/` — added them.
  `.env` was already ignored (contains real IIFL credentials).
- Full pytest suite: 23 tests pass. CLI (`python -m atr.cli`) and `import atr` both work.
- Note: `data/` is gitignored entirely, so `data/alerts/rules.json` (alert config) and
  `data/scans/` results are NOT versioned. Flagged to user; left as-is.

### Backtest performance work (~29x faster)
Profiling showed two hotspots, not the strategy logic:

1. **`pivot_to_snapshots` (src/atr/data/base.py)** — used `groupby("ts")` + `itertuples`
   per group: one group object per bar. 40.8s of a 69s run. Rewritten on numpy arrays,
   single pass over sorted timestamps; groups found via `np.unique(..., return_index=True)`.
   Affects every feed (csv, synthetic, iifl).
2. **`ctx.row()` (src/atr/strategy/base.py)** — returned `frame.iloc[i]` (a Series); each
   scalar access cost ~90us through pandas indexing. Now returns a tuple built from the
   frame's numpy columns via a cached per-column-set row class. Supports `row.close`,
   `row["close"]`, `row.get("close")`. Cache is keyed on `len(frame)` so live mode
   (where frames grow per tick) invalidates correctly.
3. **`sma_crossover`** — called `ctx.history(symbol, 2)` every bar; now uses
   `shift(1)` columns computed in `prepare()`.

Measured (2 symbols, 1-min bars, Jan–Mar 2024, 17,550 bars):
- load: 20.74s -> 0.25s · engine: 33.73s -> 1.61s · total: 54.47s -> 1.86s
- pytest: sma test 90.67s -> 5.08s; e2e test 20.27s -> 0.71s (suite ~111s -> ~6s)

**Correctness verified**: equity curve and all 1337 fills bit-identical vs the old
implementation (end equity 860304.571440, 668 trades both ways). `pivot_to_snapshots`
output compared field-by-field against the old version incl. ragged groups,
missing/None volume, empty frame.

### Walk-forward validation harness (second task)
User's stated goal: *"an intelligent but fact based system which acts on pure
statistical and strategic data, not emotions."* Interpreted as: the enemy in an
automated system is not fear/greed but **overfitting and cherry-picking**, so the
fix is statistical validation discipline, not more signals.

Added `src/atr/research/validate.py` + `atr research` CLI:
- `walk_forward()` — params chosen on train window only, scored once on the next
  unseen window; OOS equity stitched from test windows only.
- `probabilistic_sharpe_ratio()` / `deflated_sharpe_ratio()` — Bailey & Lopez de
  Prado. Uses `statistics.NormalDist` (no scipy in deps). kurtosis param is RAW
  (normal=3), so `Series.kurt()` (excess) needs +3.
- `buy_and_hold_equity()` — cost-free benchmark, therefore generous.
- `Verdict` — pass requires folds + trades + deflated Sharpe + positive OOS
  Sharpe beating buy & hold + drawdown limit.
Also added `ListFeed` to `data/base.py`.

Result: sma_crossover FAILS on synthetic data — correct, no signal in a random
walk. Diagnosed *why*: ~245 fills in 6 months x 5bps slippage ~= 12% of notional
in costs. Costs, not direction, sink it. Confirmed the harness can also PASS via
a positive-control unit test on `_verdict` (a harness that only ever says FAIL
would look like it worked).

41 tests pass (was 23). ruff clean on all touched files.

### Gotcha for future work
- In live mode `LiveRunner._ensure_frames()` calls `strategy.prepare()` on **empty**
  frames, and `_append_bar` uses `pd.concat` — so indicator columns added by `prepare`
  are present but NaN after warmup. Indicators are effectively not recomputed live.
  Pre-existing issue, not introduced by today's changes. Worth fixing before going live.
- `_RowOps` must subclass `tuple`, otherwise it lands after `tuple` in the MRO and
  `tuple.__getitem__` shadows the string-key override.
