# 2026-09-11 (Friday)

## Session + broker status

- Re-login done (daily requirement — IIFL tokens die at midnight IST). Session
  valid until 2026-09-12T00:00 IST.
- **Portfolio still blocked**: `/limits`, `/positions`, `/holdings`, `/orders`
  all return `EC500 IP address not authorized for trading.`
- Outbound public IP is **unchanged at 122.177.247.238** (same as 2026-09-10),
  so the IIFL whitelist has not been applied yet. Nothing to fix in code — it is
  an account-side setting.
- `/trades` again returned `EC926 No Trade's are found` (distinct from the IP
  error, so this endpoint is reachable; likely genuinely empty).

## Live intraday pull works (verified during market hours)

Market open 09:15–15:30 IST. Pulled all 19 scanner-universe names at 09:38:38 IST
via `market_quotes` -> `data/quotes_live_2026-09-11.csv`.

Confirmed field semantics: the `close` field in a quote is the **previous
session's close**, so `chg_pct = ltp/close - 1` is the live move vs yesterday.
(Checked: today's `close` == yesterday's `ltp`.)

Snapshot at 09:38 — 5 advancers / 14 decliners, IT up, metals + banks down:
INFY +0.46, TCS +0.45, BHARTIARTL +0.41, HCLTECH +0.38 vs TATASTEEL -2.48,
ADANIENT -1.62, LT -1.49, TITAN -1.35.

## Audit against IIFL's official GitHub org (user asked "did we miss anything?")

Org `github.com/IIFLCapital` is a **user account, not an org** — the org API
404s; use `/users/IIFLCapital/repos`. 7 repos, all market-data bridge libraries
(C#, Go, Java, .NET, Node, Python) plus one Postman collection. Only two matter
to us; the others are the same protocol in other languages.

### 1. BridgePy — FOUND TWO REAL BUGS (both now fixed, commit d5bb69a)

`bridgePy/connector.py` (52KB) is the authoritative connection protocol. It does
**not** decode payloads — it hands raw bytes to callbacks, so our hand-written
186-byte decoder was never "reinventing" anything the SDK does.

What it revealed that we had wrong:
- **MQTT auth was missing entirely.** Official: MQTT 3.1.1 + `clean_session`,
  `username_pw_set(username=<JWT preferred_username>, password="OPENID~~"+token+"~")`,
  `keepalive=20`. Ours: anonymous MQTTv5, keepalive=60. The module docstring
  already claimed auth used the JWT; the code never sent it.
- **`_handle_connect` did `int(rc)`** but paho 2.x passes a `ReasonCode`. It
  compares equal to 0, so connect *looked* fine, then raised inside the callback
  and killed the paho network thread → `is_connected` True while receiving
  nothing. Classic silent failure.

Both verified fixed against the live bridge during market hours.

Note: connector.py also contains an RSA/AES key-exchange path (struct >I lengths
for pubkey/iv/encrypted_aes_key). **Not required** — plain TLS + OPENID password
works. Probably a legacy or alternative auth mode.

### 2. Postman collection — REST coverage is COMPLETE

`data/iifl_postman_collection.json` (20KB, downloaded). 18 endpoints; our
`IiflClient` implements all of them, and the order payload matches field-for-field
(exchange, instrumentId, transactionType, quantity, product, orderComplexity,
orderType, price, validity). No response examples included, so holdings/positions
response schemas are still unknown.

**Gap found — contract files for only 3 of 9 exchanges cached:**
Official collection lists NSEEQ, BSEEQ, NSEFO, BSEFO, NSECOMM, MCXCOMM, NSECURR,
BSECURR, INDICES. We only have NSEEQ, NSEFO, INDICES in `.cache/contracts/`.
Missing: BSEEQ, BSEFO, NSECOMM, MCXCOMM, NSECURR, BSECURR — so BSE equity and
all currency/commodity trading is unreachable until synced.

## All 9 exchanges synced + contract-mapping bug found (commit 9350c65)

Synced the six missing segments: BSEEQ, BSEFO, NSECOMM, MCXCOMM, NSECURR,
BSECURR. Master now loads **207,942 contracts** (was 3 segments).

Per segment: NSEEQ 9759 · BSEEQ 13140 · NSEFO 77727 · BSEFO 37363 ·
NSECOMM 24953 · MCXCOMM 15864 · NSECURR 14602 · BSECURR 14427 · INDICES 107.

### The sync exposed a real bug (worth remembering)
`_pick()` used `row[key] not in (None, "")`. **`nan not in (None, "")` is True**,
so pandas missing values passed through: absent strikes became float NaN and
absent underlyings became the literal string `"NAN"`. And `InstrumentMaster.find()`
rebuilds a row via `DataFrame.to_dict()`, where pandas' `str` dtype stores missing
as NaN — pydantic then rejected the float for the `OptionType` enum, so **every
currency future failed to resolve** with `input_value=nan`.

Fix: added `_missing()` (None / "" / NaN / NaT), applied in `_pick`,
`_parse_expiry` and `find()`. Lesson: `not in (None, "")` is not a null check.

### Live-quote availability per segment (verified 2026-09-11, market hours)
- **Works**: NSEEQ, BSEEQ, NSEFO, BSEFO, NSECURR.
  Spot checks: RELIANCE-EQ 1259.40 · BSEEQ RELIANCE 1259.40 ·
  NIFTY26OCTFUT 23418.0 · SENSEX26SEPFUT 74475.7 · USDINR27JANFUT 89.2875.
- **NSECOMM**: responds correctly (echoes our exchange + instrumentId) but the
  probed contracts are untraded, ltp 0. Thin/legacy segment.
- **BSECURR, MCXCOMM**: `marketquotes` returns a placeholder
  `{exchange: "Z", instrumentId: 0, ltp: 0}` — **not served by the Open API**.
  Contract files exist, so they resolve, but no live prices. Not our bug.
  Note the `Z`/0 placeholder is the API's "unknown instrument" signal.

### Portfolio: STILL IP-BLOCKED (retried 2026-09-11 09:48)
Same `EC500 IP address not authorized for trading`. IP unchanged at
122.177.247.238, so the IIFL whitelist has still not been applied. This needs
the user to chase IIFL — nothing left to do in code.

Added `atr portfolio --save DIR` (one CSV per section) so the pull is ready to
run the moment access clears.

## Live path fixed — it could not place a single order (commit fdbce26)

With the bridge working, the live path became testable. It was broken in four
ways, all now fixed and covered by 11 new tests (52 pass total).

**The critical one**: `prepare()` was called once, before any live bar.
`prepare()` is a one-shot vectorised pass over a *static* frame, so every bar
appended afterwards carried NaN indicators and the strategy skipped them all.
Measured directly:
- old behaviour (prepare once at startup): **0 orders**, last-bar `sma_fast` = NaN
- fixed (refresh as bars arrive): **1 order**, last-bar `sma_fast` = 106.73

A silent no-op, not a crash — which is the worst kind for a trading system.

Also fixed:
- `LiveConfig.warmup_bars` was declared and never used. Warmup history now
  fetched via `IiflHistoricalFeed`, capped by that setting; a failing endpoint
  degrades to a cold start rather than aborting.
- `ctx.index` was set inside a per-symbol loop, so ragged frames meant one
  index could not address them all and `ctx.row()` returned the wrong bar.
  Snapshots are now appended one row per instrument (NaN where absent),
  matching `BacktestEngine._build_frames`.
- Frames grew unbounded → `max_history_bars` caps them.
- `feed` and `client` are injectable, so the loop is testable without the bridge.

**`IiflHistoricalFeed.fetch()` was also broken**: `AttributeError: 'str' object
has no attribute 'get'`. It iterated the response body instead of
`result[0]["candles"]`, so historical candles never worked through that feed.
Candles also arrive as **positional arrays** `[ts,o,h,l,c,v]`, not dicts (which
is why `scanner.py` reads them by column). Both handled. Verified live:
RELIANCE 1259.5, INFY 1041.3, zero OHLC violations.

### Verification discipline worth repeating
When adding a regression test, prove it can fail. My first attempt at
simulating the old behaviour removed `prepare()` entirely (giving a loud
KeyError) instead of calling it once (the real silent-NaN bug). Re-running the
faithful simulation is what produced the 0-vs-1 order evidence above.

## ROOT CAUSE of the IP block — it is a SEBI mandate, not an IIFL quirk

User pointed at https://developers.iiflcapital.com/. That portal blocks
automated access (Akamai "Access Denied") and its docs are a JS SPA, so
WebFetch cannot read them. agent-browser is not installed; did not install
~500MB Chromium just for this.

**IIFL's own support answer** — GitHub issue #191 on their official
`IIFL-Markets-Open-API-Postman-Collection-Official-` repo (Apr 2026), where a
user reports the *identical* `EC500 IP address not authorized for trading` on
holdings/funds and argues read-only endpoints shouldn't need IP auth. IIFL
replied verbatim:

> "Dear Madam, Kindly ensure that all API requests are made using the IP
> address registered for the clientid on our developer portal.
> Regards, App Support Broking"

They did not concede the read-only point. So the block covers account
endpoints, not just order placement.

**Where to fix it**: developers.iiflcapital.com -> Login/Sign Up -> "As
Individual Trader" -> My Apps -> the app form has a **"Primary Static IP"**
field (check via the ⋮ -> View All Details). Must equal the outbound IP, i.e.
122.177.247.238. Unclear whether it is editable post-creation; may need a new
app or support.

**Why it will not be waived**: SEBI's algo trading framework (circular Feb
2025, deferred twice) became **fully mandatory on 2026-04-01**. Every API order
must originate from a whitelisted **static** IP. One primary + one backup IP
allowed, one static IP can cover multiple brokers. Home broadband is dynamic —
"API calls will fail intermittently, so you'll need a fixed-IP setup or VPS"
(OpenAlgo docs). This is regulatory, so a support ticket will not help.

### Second live-trading blocker found while researching (fixed, 3392f77)
SEBI also requires market orders sent via API to carry a **non-zero
market-protection value**; zero or absent is rejected. `IiflBroker.build_payload`
only sent `marketProtectionPercent` when the caller passed it, so live market
orders would have been rejected. Now defaults to 0.5% for MARKET orders,
overridable per-order (`broker_params`) and per-deployment
(`IIFL_MARKET_PROTECTION_PERCENT`). Added tests/test_iifl_broker.py (10 tests)
— the broker had no tests before. 62 pass total.

Also noted: SEBI order tagging (we send `apiOrderSource=atr`, plus optional
`algoId`), and registration only matters above 10 orders/sec.

## SOLVED: the IP block was IPv6, not a missing whitelist (commit ac932fa)

User showed the portal's "Edit Details" modal: **Primary Static IP already
contained 122.177.247.238** — the correct value. So the registration was never
the problem.

Re-measuring the outbound IP was the breakthrough: four echo services returned
**two different addresses** — `122.177.247.238` (IPv4) and
`2401:4900:88f6:be42:9461:3233:3462:8bcc` (IPv6). The machine is dual-stack.

`api.iiflcapital.com` publishes both A (`23.65.124.96/99`) and AAAA
(`2600:140f:9::1741:7c60/63`, Akamai) records, so httpx was connecting over
**IPv6** — an address that was never whitelisted. The registered IPv4 was
correct and simply never used.

Decisive experiment against `/holdings`:
- default family (IPv6) -> `EC500 IP address not authorized for trading`
- `httpx.HTTPTransport(local_address="0.0.0.0")` (forced IPv4) -> **real data**

Fix: `IiflClient` now pins egress to IPv4 via `local_address="0.0.0.0"`.
Configurable: `force_ipv4` param / `IIFL_FORCE_IPV4` setting.

### Lesson
When a broker says "IP not authorized" and the registered IP looks right,
**check which address family the request actually leaves on**. A dual-stack
host silently picks IPv6. Four echo services, not one — `api.ipify.org` is
IPv4-only and reported the IPv4, which would have hidden this.

### Portfolio now retrievable (verified)
- 41 holding lots / 40 positions (CUB-EQ is two lots: 74 + 9).
- Invested 544,077 | value 611,139 | unrealised **+67,061 (+12.33%)**; 28 up / 12 down.
- Trading limit 426,669.60 | collateral margin 410,618.51 | utilised 0.
- No intraday positions, no orders, no trades today (EC920/EC916/EC926 = genuine empties).
- Top-5 = 27.2%, top-10 = 49.1% of value.
- Saved to `data/portfolio/*.csv` + `holdings_enriched.csv`.

**Merge gotcha**: sending duplicate instrumentIds to `market_quotes` returns
duplicate rows, and a naive merge then multiplies holdings rows (41 -> 43,
inflating P&L by ~8k). De-duplicate the legs and validate the merge.

## Portfolio review (user asked for an opinion)

Analysed the 40 positions from `holdings_enriched.csv`. Result is genuinely
good — profit factor 5.03 (83,698 gross gain vs 16,637 gross loss), 70% hit
rate, +12.33%.

But one structural pattern stands out and is worth remembering:
**the winners are systematically the smaller positions.**

- median position value: winners 9,587 vs losers 22,478 — losers ~2.3x bigger
- correlation(position value, % return) = **-0.231**
- top % winners are tiny: V2RETAIL +754% on a 2,180 position, HFCL +192% on
  2,337, UNITEDPOLY +111% on 4,295
- the biggest positions carry the losses: 360ONE 26,184 (-10%), PPLPHARMA
  27,898 (-1%), GOKEX 27,573 (-1.6%), KFINTECH 25,200 (-8.6%)

Also: 58% of total P&L comes from just 3 positions; 41% of capital sits idle
(426,670 margin vs 611,139 deployed); 42% of positions are under 10,000 and 12
are under 5,000 (smallest 143); 11.3% of value is precious metals
(SILVERBEES + METALIETF + GOLDBEES); 5 -BE names (trade-to-trade, no intraday)
at 3.4%.

Caveat stated to the user: entry dates are not in the holdings response, so
"deliberately small" cannot be distinguished from "scaled out". Worth noting if
we ever build a position-sizing rule.

## Buy/sell signal system + Telegram (commit 48eaaac)

Built `src/atr/signals/` at the user's request: tell me when to buy, check my
holdings for sells, message me on Telegram.

**Architecture decision that matters**: the entry rules are exposed as
`SignalEntryStrategy`, which calls the *same* `eval_entry`/`eval_exit` functions
the live scanner uses. Separate implementations would make a validation pass
meaningless.

- Sell side (`atr signals sell`): risk rules over actual holdings — stop_loss,
  trailing_stop, confirmed trend_break, take_profit, rsi_overbought. Risk
  management, not prediction, so no edge required.
- Buy side (`atr signals buy`): entry rules over a watchlist, marked
  `unvalidated` until `atr signals validate` passes.

### The honest result: the entry rules FAIL out of sample
5 folds, 247 trades, **-5.05%** vs buy-and-hold **+57.39%**, Sharpe -0.06 vs
0.85, deflated Sharpe 0.926 below the 0.95 bar. The scanner reads
`data/signals/validation.json` and labels every buy signal unvalidated. Do not
present these as actionable.

### Two real bugs fixed while building
1. **`rsi()` was wrong on a monotonic rise.** `loss.replace(0, np.nan)` made a
   zero average loss NaN, then `fillna(50)` reported it as perfectly neutral.
   The strongest possible uptrend read RSI 50 — which silently qualified it as
   an RSI *pullback*. Now returns 100. This affected the scanner, the alerts,
   and every RSI-based rule. Locked down in tests/test_indicators.py.
2. **`alerts.fetch_quotes` mapped quotes positionally**, so two rules on one
   symbol misattributed the response. Now keyed by (exchange, instrumentId).

### Tuning lesson
`trend_break` fired on 10 of 17 holdings with a single close below SMA50 —
alert fatigue, not information. Added `trend_confirm_bars` (default 3
consecutive closes); now 8. Any new rule should be checked for how many
positions it flags before being trusted.

### Validation gotchas found
- The local daily cache holds only ~1 year — too short to carve into folds that
  also leave room for warmup. `atr signals validate` now fetches ~6 years
  (1,497 bars) per symbol. The API will serve 6y of dailies.
- `min_history_bars` must be comfortably smaller than the walk-forward test
  window or the strategy trades **zero times**, and "no trades" looks identical
  to "no edge". First run with `min_history_bars=210` and `test_bars=40`
  produced exactly 0 trades.

### Automation
Created recurring automation `14bee75a-5b05-4853-9846-c14e2385b5b9`
("ATR buy/sell signals to Telegram"): hourly on weekdays with a market-hours
guard in the prompt. **Constraint**: multi-hour RRULE is not expressible
(comma-separated BYHOUR is rejected), hence hourly + guard.

**Important**: the IIFL session dies at midnight IST and needs an interactive
auth code, so the automation can only succeed on days the user logs in. The
prompt handles this by sending a "session needs renewing" Telegram rather than
trying to re-authenticate.

## Next
- Tick streaming (`atr live`) is only meaningful during market hours — this is
  the window to exercise it.
- `atr brief` (morning briefing -> Telegram) not yet run this session.
